To compile the program, run `gcc --std=gnu99 -o smallsh main.c`. To run the
executable (`smallsh`), run `./smallsh`. You can also test the program with the
grading script using `./p3testscript 2>&1`.
